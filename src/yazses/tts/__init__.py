"""Offline text-to-speech for the Read-Back Loop (spec-read-back-loop).

Permissive engines only (Kokoro Apache-2.0 default; MeloTTS/KittenTTS). All deps
live in the optional ``tts`` extra and are imported only when ``[tts] enabled``.
"""
