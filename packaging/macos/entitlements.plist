<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for sounddevice / PortAudio mic capture. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!--
        PyObjC and bundled CTranslate2 native libs are not signed by Apple,
        so under hardened runtime we have to disable library validation.
        Only relevant once we sign + notarize; harmless in unsigned v0.
    -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Allows mode-dispatched subprocess spawn of the daemon. -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
</dict>
</plist>
